# Carrega o arquivo .RData
load("resultados/simulacao01.RData")

# Verifica o conteúdo do arquivo carregado
ls()  # Lista os objetos no ambiente

# Acesse os objetos individuais
print(distribution)
print(lambda)
print(obs)
print(result)
